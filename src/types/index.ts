export enum UserRole {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum UserStatus {
  VERIFIED = 'VERIFIED',
  UNVERIFIED = 'UNVERIFIED',
  BLOCKED = 'BLOCKED',
}

export enum Status {
  ACTIVE = 'ACTIVE',
  DELETED = 'DELETED',
}
